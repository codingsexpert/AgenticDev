

# Git Workflow Rule
Whenever you implement a new feature, fix a bug, or make significant changes, ALWAYS commit the changes to Git and push to GitHub (using `git add .`, `git commit -m "..."`, and `git push`) before finishing the task.
